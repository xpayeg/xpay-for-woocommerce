# macOS
.DS_Store
._*

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Build / dependencies
node_modules/
vendor/

# Build outputs (produced by bin/build.sh — see docs/PACKAGING.md)
dist/
build/

# Logs
*.log

# Environment
.env
.env.local
.phpunit.result.cache

# Left behind by `npm install playwright-core` for the browser tests.
# A dev driver, never a plugin dependency (tools/browser-tests/README.md).
package.json
package-lock.json

# Screenshots the browser tests write; evidence, not source.
tools/browser-tests/screenshots/
